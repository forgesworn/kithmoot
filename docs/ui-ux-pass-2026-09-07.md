# UI/UX pass — 7 September 2026

The target is a workspace that makes everyday conversation easier: enter a
room, find the right conversation, catch up, reply, and return to work without
losing context. “Beat Slack” needs evidence from those journeys and people
using them; a feature list or passing browser suite does not establish it.

## Catching up

The existing UI had visible unread badges, but selecting a conversation
always jumped to the bottom and cleared them. Switching away while reading
also lost the place. That made the badges poor guides in busy conversations.

This change:

- Remembers the first visible message and its offset for each conversation
  during a room visit, before changing the toolbar or composer layout.
- Opens unseen activity at a New messages divider. An existing reading
  position takes precedence, so returning does not interrupt reading.
- Offers Next unread with the destination and count, including conversations
  beyond the edge of the phone's navigation strip.
- Keeps Jump to latest available when reading above the bottom; keyboard
  focus returns to the conversation after using either catch-up action.
- Counts resolved messages instead of treating edits as extra unread messages.
  Reactions, retractions and private invitation events do not inflate the count.
- Keeps long destination names within the phone layout; the full label remains
  the accessible name and tooltip.
- Keeps catch-up and search on one row on short phones, with picture
  preferences still available in Room details. Empty composers use one line
  even when the conversation name makes the placeholder long.

Reading positions and conversation unread counts are scoped to this room
visit. This does not add cross-room history loading or persist draft text.
The divider stays in place while reading and resets on switching conversations.
Existing bottom-of-conversation read semantics remain: reaching the bottom
marks the loaded conversation read when it is visible and no dialog covers it.

## Finding messages across a room

Search is now visible in the conversation toolbar and covers all loaded room
conversations by default. Results name their conversation; selecting one opens
the original message in context, including the latest version of an edited
message and nested replies. Retracted messages do not appear.

A conversation filter and Files only narrow the results. Back to results keeps
the query, filter and result position while moving between contexts. The first
100 results have an explicit Show more action. Incoming messages update results
without stealing focus; closing search clears the query. Room details retains
the shortcut for searching only the current conversation.

Search reads already loaded, decrypted history: up to 500 messages per
conversation from the last 30 days. It does not load other rooms or fetch files.
The coverage explanation is available inside search. Phone layouts cover both
themes, long conversation names and 200% text; Safari reopening uses the search
field as the dialog's initial focus target.

## In-app confirmations

The app's 16 browser `confirm()` calls now use a shared styled dialog. Each
question has a descriptive action, a safe initial Cancel/Keep working button,
keyboard focus containment, Escape and backdrop cancellation, and focus return.
Destructive actions use the existing danger colour. Long room names and the
browser-room import list wrap inside a scrolling dialog on small screens.

Messages and calls continue while the person decides. Room and account changes
invalidate pending room actions. Pairing now awaits approval, remembers each
device decision for the invitation's lifetime, and ignores duplicate requests
while a question is pending. Closing the pairing host prevents an outstanding
approval or signer response from issuing a credential. A fresh invitation
allows a cancelled pairing to be tried again.

Draft discard, leaving, room switching, bookmark import/forget, message
retraction, failed-send dismissal, room administration, invitation replacement,
device pairing and update/reload use the same component. Browser-controlled
`beforeunload` remains for an actual tab close or browser reload with unsent
work; a website cannot customise that warning.

## Reading a busy conversation

Each writable message now has one visible actions button in place of repeated
Reply, Edit/Retract and three unused reaction buttons. The anchored action panel
contains Reply, the author's edit/retract controls, and all eight supported
reactions. Reactions that people have used stay directly on the message,
including the existing agent receipt details.

Consecutive messages share the visible sender heading when their author, claimed
name, owner, agent status and message kind agree, and they are less than five
minutes apart on the same day. Matching display names alone never group people.
Threads, system lines, retractions and unread dividers preserve clear boundaries.
Every message retains its timestamp and accessible sender identity.

The action panel stays attached to its original message while the log repaints,
updates its excerpt after edits, and closes if that message disappears or is
retracted. Escape returns focus to the message; Tab reaches every action;
clicking back into the composer dismisses it without changing the draft.
Selecting Reply or Edit moves focus into the composer. Reply previews resolve
the latest text, and long names wrap at enlarged text sizes without pushing
Cancel beyond the screen. An open action panel does not mark covered arrivals
read automatically.

## Remaining work in the wider pass

| Journey | Current evidence | Next improvement to assess |
| --- | --- | --- |
| Move between rooms with work in progress | Workspace tests confirm a draft routes switching through the protective picker | Assess a smoother multi-room journey while retaining drafts and live-call safeguards |
| Get started and collaborate | Existing home, room, agent and Work journeys have separate acceptance suites | Revisit the complete entry-to-first-use flow and ensure the main actions explain themselves |
| Trust the result on a phone | Responsive browser renders cover narrow screens and large text | Physical phone, installed PWA, keyboard and assistive-technology acceptance remain separate |

## Validation

Node 24: type checking and the production build pass; all 1,379 unit tests pass, including asynchronous pairing approval and cancellation.
The production build retains the existing JavaScript chunk-size warning.

The new workspace browser journey exercises a busy room, background agent
messages, a preserved draft, arrival during catch-up, returning to the same
visible message, Jump to latest, editing a read message, a read-only
conversation, long channel names and a 320 × 540 viewport. Existing workspace,
draft, search and message-reliability checks are run alongside it.

The final workspace, draft, search and message-reliability run passed 35 checks
in Chromium/Firefox and 17 in WebKit. The two skips reserve the microphone
search test for Chromium, where it passed. The new catch-up test was then
tightened to verify editing while reading above the bottom, so automatic
marking cannot conceal a false unread count; it passed in all three engines.

Screenshots and traces are under `/tmp/kithmoot-ux-complete-browser` and
`/tmp/kithmoot-ux-complete-webkit`; the tightened catch-up test uses
`/tmp/kithmoot-ux-unread-regression` and `/tmp/kithmoot-ux-unread-webkit`.
They include desktop and phone layouts, both themes, large text, and the
320 × 540 catch-up flow. Baseline screenshots are in `/tmp/kithmoot-ux-before`.

The search and confirmation follow-up adds room-wide results, edited/retracted
messages, paginated results, background arrivals, draft preservation, keyboard
and 200% text checks. The final Chromium/Firefox search, confirmation and
message-reliability run passed 21 checks with one microphone skip. Pairing
cancellation and approval were driven on two independent browser contexts.
The persistent-group follow-up passed all eight Chromium/Firefox checks.
Existing draft, message, bookmark, room-switching and PWA update checks also
passed; microphone cases remain Chromium-only.

The wider WebKit run passed 29 checks with two microphone skips. The final
confirmation checks then passed in all three engines: four Chromium/Firefox
checks and two WebKit checks. These include explicit Tab navigation between
both decisions, initial safe focus, Escape and backdrop cancellation, incoming
messages while waiting, 200% text, and pairing rejection followed by a fresh
invitation. Three Chromium paired-device roster and live-media checks passed.

Earlier failed checks were investigated and rerun: rebuilding the local preview
mid-run caused a 404; storage assertions needed to await asynchronous dismissal;
a pairing fixture needed a full navigation for its new invitation; and WebKit's
default tab order skipped buttons, now handled by the confirmation component.

Follow-up evidence is under `/tmp/kithmoot-dialog-final`,
`/tmp/kithmoot-dialog-persistent-final`, `/tmp/kithmoot-dialog-acceptance`,
`/tmp/kithmoot-dialog-webkit`, `/tmp/kithmoot-dialog-keyboard-final`,
`/tmp/kithmoot-dialog-keyboard-webkit` and `/tmp/kithmoot-dialog-pairing-media`.

The busy-conversation follow-up passed 57 browser checks in Chromium/Firefox
and 28 in WebKit, with one microphone skip in each run. These cover the new
panel and grouping alongside search, catch-up, drafts, confirmations, message
operations and agent receipts. The tightened covered-arrival check then passed
in all three engines. Type checking and the production build pass.

The new journey verifies different people with matching names, a five-minute
break, every timestamp, only used reactions in the log, all eight reaction
choices, keyboard focus through incoming edits, cancellation, stale-message
retraction, and reply context at 320 pixels and 200% text. Screenshots cover
both themes at 320, 390 and 1440 pixels. Evidence is in
`/tmp/kithmoot-density-verified`, `/tmp/kithmoot-density-verified-webkit`,
`/tmp/kithmoot-density-unread` and `/tmp/kithmoot-density-unread-webkit`.

On this macOS 14.6.1 host, Playwright 1.62.1 still fails before page creation
with `Unknown setting: PushAPIEnabled`. WebKit therefore uses the installed
v2251 browser through the isolated Playwright 1.58.2 runner at
`/tmp/kithmoot-ux-webkit-20260907`, with copies of the current test sources and
the same production build at `https://localhost:4187/j/`. Repository dependency
versions are unchanged. This is browser-engine evidence, not physical Safari
or installed-PWA acceptance.

Reproduce the standard browser checks with the local test relay and a preview
server, or omit `E2E_BASE_URL` to let Playwright start them:

```sh
E2E_BASE_URL=https://localhost:4187/j/ npx playwright test \
  test/workspace.spec.ts test/chat-reliability.spec.ts \
  test/conversation-search.spec.ts test/drafts.spec.ts test/confirmations.spec.ts \
  --project=chromium --project=firefox
```

Changes are local to `feat/ux-catchup`; this pass has not deployed the app.
