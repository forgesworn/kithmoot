# Desktop preview 0.1.10 — 20 September 2026

Desktop chat and its composer now fill the available room width. The area-sharing
frame has four 28px corner handles, a clear draggable Move bar, and arrow-key
movement/resizing. Its native window uses the screen-saver always-on-top level
and is visible across macOS workspaces, including fullscreen spaces. The crop
excludes the wider border, toolbar and footer.

Drawing labels now form one bottom colour legend entry per author, instead of
one label per transmitted segment. Entries fade with that author's last visible
stroke and disappear on expiry or clear. Capture cancellation still stops late
screen/audio tracks, and a cancelled or replaced native frame cannot complete an
older pending source enumeration.

The Mac archive is Developer ID signed and notarised. Linux x64 and ARM64
archives retain the preview distribution model.

## Packaging and signing

The packager's obsolete `signAsync` import prevented packaging with the pinned
`@electron/osx-sign` 2.7.0. It now uses that library's documented async `sign`
export. Both the real signing run and packaged-app smoke completed.

The account holder created a Developer ID Application certificate from a local
CSR. The existing App Store Connect API key successfully authenticated to the
notary service. App Store signing identities were retained; the new identity
is installed in a dedicated local release keychain. Private keys and credentials
are outside the repository.

- Identity: Developer ID Application: Epona Solutions Ltd (2GLP954N6A).
- Bundle: `dev.forgesworn.kithmoot.desktop`, version 0.1.10.
- Hardened runtime and secure signing timestamp verified.
- Apple submission `3532a04c-ac3a-4f6e-905f-6c6a38c55a60`: **Accepted**.
- Stapling and ticket validation passed.
- Gatekeeper: **accepted**, source **Notarized Developer ID**.
- Final filenames, byte sizes and SHA-256 hashes are in
  [release.json](../../site/downloads/release.json).

## Candidate checks

- Typecheck passed; 37 focused layout/annotation unit tests and ten desktop
  boundary/geometry checks passed.
- Twelve Chromium/browser-desktop checks passed: full-width chat, reading
  position, folding the rail, media layouts, drawing delivery/colour/expiry,
  cropped capture, audio-track handling and late capture cancellation.
- The updated native Mac/browser journey passed pointer resizing, keyboard
  movement, opposite-corner anchoring, always-on-top/workspace flags, invalid
  bounds rejection and exact 8/52/8/40 capture insets. Chat/composer width and
  retained drafts passed at 1920, 1320 and 1100 pixels, including rail/Work changes.
- The first native run timed out waiting for remote video before reaching the
  new controls. Rebuilding the production desktop assets and rerunning alone
  passed in 15.3 seconds. This is an observed intermittent test failure, not
  evidence that every call always establishes successfully.
- Native frame and browser legend screenshots were inspected. The legend sits
  at the bottom and contains one coloured name entry, with no labels on strokes.

- Native Electron smoke passed on the development build and the signed packaged
  app: frame creation, resize, chooser gesture, closure, sandbox bridge,
  no renderer Node APIs, CSP, traversal denial and profile persistence.
- All five native/browser journeys passed: bidirectional synthetic audio/video
  and chat; cancelling close and leaving a call; notification privacy and badge
  behaviour; room switching and separate drafts; relay-only failure handling.
- Linux package versions and ELF architecture headers verified for x64 and
  ARM64. All three platform archives carry app.asar SHA-256
  `10ea5b155aad0213e9fbaace5f659e6c55e6f9b87d5365304ebd0e8a40956efd`. This release's Linux archives
  have not been run on a physical Linux desktop.
- Capture cancellation and crop/audio browser checks are recorded in the
  [capture fix review](2026-09-20-desktop-capture-cancellation.md).

## Remaining acceptance

Actual display capture, audible system sound, remote-call echo exclusion,
drawing alignment and permission continuity need physical Mac checks.
A first upgrade from an ad-hoc preview may require granting permission again.
There is no automatic updater. Signing and notarisation do not close these
product acceptance checks.

Publication and deployment must be verified against these exact artifact hashes
separately from the candidate evidence above.

## Worker assistance

The ollama-workers skill supplied bounded drafts; Codex reviewed and integrated
accepted work and owned native IPC/capture/signing decisions. No credentials or
private chat screenshots were sent to cloud workers.

| Model | Thinking | Work and disposition | Reported prompt / completion tokens |
| --- | --- | --- | --- |
| qwen3.8:latest | false | Backlog draft; rejected unsupported CI attribution | 6962 / 1399 |
| qwen3.8:latest | false | Capture review; rejected incorrect guards | 2918 / 882 |
| qwen3.8:latest | false | Native chat geometry test draft; repaired and accepted | 358 / 625 |
| deepseek-v4.1-flash:cloud | false | Layout review; accepted Work/rail test suggestions, rejected unsupported CSS diagnosis | 5227 / 1271 |
| glm-5.3-flash:cloud | low | Synthetic native chat screenshot inspection; corroborated visible layout | 2912 / 377 |
| deepseek-v4.1-flash:cloud | false | Legend helper and tests; reviewed, cleaned up, integrated and tested | 3714 / 3161 |

The two bounded rounds used three calls each. All returned terminal responses.
Reported totals: 13,144 local Qwen tokens and 16,662 cloud tokens. These are
provider-reported token counts, not an invoice or a cash-savings claim.
