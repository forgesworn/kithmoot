# Website Android update: account chats and rooms

Alex obtained the website APK. The live manifest still advertised 0.5.12/code20, while the signed Pixel candidate with account-room sync was 0.6.2/code25. This was a distribution gap, not evidence of a newly reproduced account-sync regression.

Published the already user-signed M4 artefact from kithmoot-leave-call-20260916/source/app/build/outputs/apk/release/kithmoot-0.6.2-production.apk. No signing key or password was read or copied. SHA-256 50ceaf8672a0c777ed33eba46acb6b51087a68c929bfbdefde01e55d1d4157b0. Certificate 135bcabf715216d13b7a596c6f891c3220a7f90826ef8e75930d51b34506a2e9. v3 only; minSdk33, target35. Android 13+ is now explicit on both download pages. The old preview remains at its immutable URL for Android8–12, with limitations stated.

The repository publication verifier passed against the exact APK, metadata and homepage. Its two-certificate lineage starts with the website preview certificate 5a04a77faf9d747728b8d74175225aa1920add75371a9d566d801fe55e390874 and retains installed-data and permission capability, with sharedUid/rollback/auth false for the previous signer.

Disposable API35 ARM64 emulator, kithmoot-public-upgrade-20260916, port5580: installed the exact public 0.5.12 APK, created a synthetic guest room using a localhost test relay, then installed 0.6.2 with adb install -r without uninstalling. Android accepted the signing rotation. The saved room remained listed and could be opened, demonstrating recovery of encrypted saved-room storage across the upgrade. The reopened native UI screenshot was visually checked. The production app did not connect to the cleartext test relay, so this check does not claim post-upgrade network delivery. The attempted pre-upgrade test message remained a composer draft, so it is not evidence of retained message history. Prior signed-candidate build evidence records 391 JVM tests, lint and three emulator call/leave tests; account-sync instrumentation already exists in AccountExperienceUiTest. No new physical Alex acceptance is claimed.

Website release /var/www/kithmoot/releases/20260916-android-account-sync copies 20260916-paste-privacy and updates only index.html, android-release.json and downloads/index.html. Full /j/ tree unchanged, including the earlier clipboard fix. Desktop release metadata and checksums unchanged. The APK was SHA-256 checked before moving from an incoming file to immutable apk/kithmoot-0.6.2-production.apk. Stable APK and current-site symlinks were switched with rollback on activation failure. No Caddy changes, service restart, commits, pushes, Pixel installation or personal-device automation.

Alex should choose Update, not uninstall, on Android13+. Use the same Nostr account and overlapping relays, then Chats. Browser-only rooms still require adding to the synced account; bookmarks do not carry a complete message archive and room admission/history availability still apply.

Post-deploy verification: the stable public HTTPS APK fully downloaded (112,888,247 bytes) and matched the expected SHA-256. Live release metadata reports0.6.2/code25. Homepage/downloads passed six browser layout checks at320/390/1440px with no page errors or horizontal overflow; Linux disclosure still works. Disposable emulator and local relay stopped after checks.
